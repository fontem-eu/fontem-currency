apiVersion: v1
kind: Service
metadata:
  name: fontem-currency
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: fontem-currency
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
